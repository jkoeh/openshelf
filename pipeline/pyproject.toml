[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "openshelf"
version = "0.1.0"
description = "Open source public domain audiobook platform"
requires-python = ">=3.11"

dependencies = [
    "kokoro",
    "f5-tts",
    "chatterbox-tts",
    "soundfile",
    "numpy",
    "pydub",
    "ebooklib",
    "beautifulsoup4",
    "boto3",
    "anthropic",
    "openai",
    "python-dotenv",
    "torch",
]

[project.optional-dependencies]
dev = [
    "ruff",
]

[project.scripts]
openshelf-gpu-preflight = "openshelf.pipeline.gpu_preflight:main"
openshelf-pipeline-doctor = "openshelf.pipeline.pipeline_doctor:main"
openshelf-pipeline-runner = "openshelf.pipeline.pipeline_runner:main"
tts-pipeline = "openshelf.pipeline.runner:main"

[tool.setuptools.packages.find]
where = ["src"]
